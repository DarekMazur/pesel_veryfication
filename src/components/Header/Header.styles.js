import styled from "styled-components";

export const StyledHeader = styled.header`
    padding: 1rem;
		text-align: center;
		min-height: 10rem;
		display: flex;
		align-items: center;
		justify-content: center;
`